<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
                xmlns:tools="http://schemas.android.com/tools"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:paddingLeft="@dimen/activity_horizontal_margin"
                android:paddingRight="@dimen/activity_horizontal_margin"
                android:paddingTop="@dimen/activity_vertical_margin"
                android:paddingBottom="@dimen/activity_vertical_margin"
                tools:context="com.microblink.ocr.MainActivity">

    <Button
        android:id="@+id/btnSimple"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:onClick="simpleIntegration"
        android:text="Simple integration"/>

    <Button
        android:id="@+id/btnCustomUI"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:onClick="advancedIntegration"
        android:text="Custom scan UI integration"
        android:layout_below="@+id/btnSimple"
        android:layout_centerHorizontal="true"/>

    <Button
        android:id="@+id/btnRegex"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:onClick="regexExample"
        android:text="Regex parser example"
        android:layout_below="@+id/btnCustomUI"
        android:layout_centerHorizontal="true"/>

</RelativeLayout>
